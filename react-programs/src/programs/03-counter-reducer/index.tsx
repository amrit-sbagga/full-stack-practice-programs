/**
 * PROGRAM 03 — Counter using useReducer
 *
 * Same counter UI as Program 01 (Increment / Decrement / Reset),
 * but state must be managed with useReducer instead of useState.
 *
 * Define a reducer function and dispatch actions for each button.
 */

export default function CounterReducer() {
  return <div />;
}
