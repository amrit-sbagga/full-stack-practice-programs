/**
 * PROGRAM 04 — Counter using Context API
 *
 * Same counter UI as Program 01 (Increment / Decrement / Reset),
 * but the count state must be shared via React Context.
 *
 * Requirements:
 *   - Create a CounterContext and a CounterProvider component
 *   - Create a separate DisplayCount child component that reads count from context
 *   - Create a separate Controls child component that calls increment/decrement/reset from context
 *   - The parent component should only render the Provider and the two children
 */

export default function CounterContext() {
  return <div />;
}
