/**
 * PROGRAM 10 — Change Page Background Color
 *
 * Render a row of color buttons. Clicking a button changes the background
 * color of a preview area on the page.
 *
 * Colors to offer: White, Red, Blue, Green, Yellow, Purple
 *
 * Requirements:
 *   - The currently active color button should look visually selected
 *   - Display the name of the active color inside the preview area
 */

export default function ColorChange() {
  return <div />;
}
