/**
 * PROGRAM 11 — Form Validation
 *
 * Build a form with three fields and validate on submit.
 *
 * Fields and rules:
 *   Name   — required, between 3 and 30 characters
 *   Email  — required, must be a valid email format
 *   URL    — required, must start with http:// or https://
 *
 * Requirements:
 *   - Show an inline error message below each invalid field
 *   - On successful submission show a green success message
 *   - On failure (any invalid field) show a red "Form not submitted" message
 *   - Do not submit if any field is invalid
 */

export default function FormValidation() {
  return <div />;
}
