/**
 * PROGRAM 16 — tRPC + Express
 *
 * tRPC is normally implemented in TypeScript. See README.md in this folder for the
 * full requirement list, suggested packages, and how to run with tsx.
 *
 * Summary:
 *   - Shared "Book" domain with programs 13–14 (GraphQL) and 15 (gRPC)
 *   - Express on PORT **3016**, mount tRPC at /trpc
 *   - Procedures: list books, get by id (NOT_FOUND if missing), create with Zod validation
 *   - Seed 2 books in memory
 *
 * This index.js is a stub until you add server.ts and wire npm run 16.
 *
 * Run: npm run 16
 */

console.log(
  "Program 16 — tRPC: read programs/16-trpc-server/README.md and add server.ts + deps.",
);
process.exit(0);
